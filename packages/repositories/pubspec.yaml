name: repositories
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  get_it: ^7.7.0
  firebase_auth: ^4.19.2
  services:
    path: ../services
  models:
    path: ../models

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

